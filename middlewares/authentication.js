const { verifyToken } = require('../helpers/jwt');

module.exports = {
    authentication(req, res, next) {
        let { token } = req.headers;

        try {
            
        } catch (error) {

        }
    }
}